/**
 * Background job + queue definitions backed by pg-boss.
 * Handlers and queue names live under ./handlers and ./queues — added per milestone (M6+).
 */
export const JOBS_PACKAGE_VERSION = '0.0.0';
